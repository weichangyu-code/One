

class HttpServer
{
    void main()
    {
        Socket sockListen();
        sockListen.listen("0.0.0.0", 8000, true, 128);

        while (1)
        {
            Socket newClient();
            if (newClient.accept(sockListen, -1) == 0)
            {
                Out.println("new link");
                HttpLink link(newClient);
            }
        }
    }
}
